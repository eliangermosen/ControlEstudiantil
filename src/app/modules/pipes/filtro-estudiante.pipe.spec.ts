import { FiltroEstudiantePipe } from './filtro-estudiante.pipe';

describe('FiltroEstudiantePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEstudiantePipe();
    expect(pipe).toBeTruthy();
  });
});
